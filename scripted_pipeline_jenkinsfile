
pipeline {
agent any
stages {
    stage('Build in Docker') {
        steps {
            script{
                               
                sh "docker run --name demo -i httpd bash -c 'ls'"
            }
        }
    }
}
}
