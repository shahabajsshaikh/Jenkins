
pipeline {
agent any
stages {
    stage('Build in Docker') {
        steps {
            script{
                sh 'docker run -d --name demo1 shahabajsshaikh/openjdk8:0.0'
                sh "docker exec -it demo1 bash -c 'ls'" 
            }
        }
    }
}
}
