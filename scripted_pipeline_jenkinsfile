
pipeline {
agent any
stages {
    stage('Build in Docker') {
        steps {
            script{
                sh 'docker run -d --name demo2 shahabajsshaikh/openjdk8:0.0'
                sh "docker exec -it demo2 bash -c 'ls'" 
            }
        }
    }
}
}
