
pipeline {
agent any
stages {
    stage('Build in Docker') {
        steps {
            unstash 'scm'
            script{
                docker.image('httpd').inside{ 
                    sh 'pwd'
                }
            }
        }
    }
}
}
