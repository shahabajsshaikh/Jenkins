
pipeline {
agent any
stages {
    stage('Build in Docker') {
        steps {
            script{
                sh 'docker run -d --name demo5 shahabajsshaikh/openjdk8:0.0'
                sh "docker exec -i demo5 bash -c 'ls'" 
            }
        }
    }
}
}
